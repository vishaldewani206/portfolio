# My Portfolio

### Setup

Clone the repo:
``` git clone ```

Install the dependencies:
``` npm install ```

Build or watch the CSS file:
```
# Build once
npm run build

# Watch for changes
npm run watch
```
